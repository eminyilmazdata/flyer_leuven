/** Default campaign created by seed; slug used to resolve campaign on the public board. */
export const DEFAULT_CAMPAIGN_SLUG = "leuven-center";

export const SESSION_COOKIE = "fl_session";
export const COORDINATOR_COOKIE = "fl_coordinator";

export const SESSION_MAX_AGE_SEC = 60 * 60 * 24 * 30; // 30 days
export const COORDINATOR_MAX_AGE_SEC = 60 * 60 * 24 * 7; // 7 days
